<h1>Album Collection</h1>

<ul>
  <% @albums.each do |album| %>
    <li>
      <%= link_to album.properties['title'], album_path(album.discogs_id) %>
      <div> <img src="<%= album.properties["thumb"]%>"> </div>
    </li>
  <% end %>
</ul>
