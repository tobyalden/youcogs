<h2>Search Results:</h2>

<ul>
  <% @search.results.each do |result| %>
    <li>
      <%= link_to result['title'], album_path(result['id']) %>
      <div> <img src="<%= result["thumb"]%>" > </div>
      <hr>
    </li>
  <% end %>
</ul>
